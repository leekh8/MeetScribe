# 핵심 (Phase 1) — 오프라인 전사
faster-whisper>=1.2.0

# 화자 분리 (Phase 2) — 필요할 때 주석 해제. HuggingFace 토큰 + 모델 동의 필요.
# pyannote.audio>=3.1

# LLM 요약 (Phase 3) — 필요할 때 주석 해제.
# anthropic>=0.40

# UI (Phase 4)
# streamlit>=1.30

# ffmpeg는 별도 설치 (winget install ffmpeg / apt install ffmpeg)
